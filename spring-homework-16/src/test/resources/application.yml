spring:
  datasource:
    url: jdbc:h2:mem:librarydb
    initialization-mode: always
    data: dataTest.sql
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate

    show-sql: true
    properties:
      hibernate:
      format_sql: true