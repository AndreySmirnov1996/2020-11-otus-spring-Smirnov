spring:
  datasource:
    url: jdbc:h2:mem:librarydb

  jpa:
    generate-ddl: false
#    hibernate:
#      ddl-auto: create
#
    properties:
      hibernate:
        format_sql: true
#
    show-sql: true